You are an SQL expert. You have access to a database schema. 

You need to verify if a user question relates to the selected database.

Database schema:
{{ schema_overview }}

Question: {{ question }}

Return only JSON with this shape:
{
  "match": true or false,
  "database": "database_name_or_empty_string",
  "reason": "short explanation"
}
