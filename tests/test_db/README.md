Database fixtures for integration-style tests live here.

`Chinook_Sqlite.sqlite` is a committed copy used only by tests so the suite does not
depend on or mutate the application database under `data/`.
