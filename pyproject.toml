[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sql-analyser-copilot"
version = "0.1.0"
description = "A LangGraph-based SQL copilot for SQLite analysis."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "jinja2>=3.1,<4.0",
    "langgraph>=0.2,<1.0",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
